require "spec_helper"

describe GasLoadTester do
  it "has a version number" do
    expect(GasLoadTester::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
